<div class="box box-primary">
  <div class="box-header">
    <i class="fa fa-cloud"></i>
    <h3 class="box-title">OpenStack Clusters</h3>
  </div>
  <div class="box-body table-responsive no-padding">
    <table class="table table-hover">
      <thead>
        <th>Name</th>
        <th>Status</th>
        <th>Health</th>
        <th>Details</th>
        <th><abbr title="Intel&reg; Datacenter Manager: Service Assurance Administrator">SAA</abbr> Controllers</th>
      </thead>
      <tbody>
        {{#each}}
          <tr>
            <td>{{name}}</td>
            <td>{{status}}</td>
            <td>{{systemHealth}}</td>
            <td>{{{details}}}</td>
            <td>
              {{#if ipms}}
                {{#each ipm in ipms}}
                  {{ipm.name}}
                {{/each}}
              {{else}}
                Not assigned
              {{/if}}
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
